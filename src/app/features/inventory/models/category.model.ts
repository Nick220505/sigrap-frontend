export interface CategoryInfo {
  id: number;
  name: string;
  description?: string;
  createdAt?: string;
  updatedAt?: string;
}

export interface CategoryData {
  name: string;
  description?: string;
}
