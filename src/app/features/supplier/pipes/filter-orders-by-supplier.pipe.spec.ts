import { FilterOrdersBySupplierPipe } from './filter-orders-by-supplier.pipe';

describe('FilterOrdersBySupplierPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrdersBySupplierPipe();
    expect(pipe).toBeTruthy();
  });
});
